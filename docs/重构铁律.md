read
https://facebookresearch.github.io/meta-agents-research-environments/api_reference/agents.html#core-agent-classes
See how class MyCustomAgent is defined. We mimic that.

You can look at these two files on creating an agent and an executor loop: are/simulation/agents/default_agent/base_agent.py  and are/simulation/agents/default_agent/are_simulation_main.py.
I realized that maybe you will need to create another class similar to BaseAgent for the UserAgent class because otherwise the agent builder will not build the agents properly.

But yeah look into the Meta-Are codebase deeply before changing the code.

可以使用llm planner 组合

› 现在我们要重构 user proxy,主要是要复用 meta are的设施.你可以看一下https://facebookresearch.github.io/meta-agents-research-environments/api_reference/agents.html.然后,传入的是 tools,env是不能被传入 agent的.def _find_tool和def_execute_tool都是不需要的.变量名不需要加_,但是函数名可以加_你不用改.请你仔细研究方案,告诉我一个好的改法.

你看看 baseagent其它部分都是怎么做的,我们完全应该学习它或者使用它的约定。

我们要尽量复用原生设施 因此代码一定会尽量精简。
